### Steps to install and run Jekyll locally
```
gem install bundler
git clone https://github.com/CivicActions/CivicActions.github.io.git
cd CivicActions.github.io
bundle install
bundle exec jekyll serve
```
